# Porcupine system monitor — configuration file
# Copy to /etc/porcupine/porcupine.conf and edit as needed.
# CLI flags always override values set here.

[monitors]
# How often each monitor appears in the display cycle.
# 0 = disabled, 1 = every cycle (default), 2 = every other cycle, 5 = every 5th cycle, etc.
boot_every  = 10
power_every = 5
cpu_every   = 5
temp_every  = 1
net_every   = 10
gpio_every  = 2

[hardware]
# I2C address of the LCD backpack (PCF8574). Use hex (0x27) or decimal (39).
lcd_addr    = 0x27
# BCM GPIO pin numbers
button_pin  = 4
buzzer_pin  = 18
# I2C address of the INA219 power/battery monitor (0x41–0x43 depending on A0/A1 pins).
ina219_addr = 0x41

[display]
# Seconds between automatic screen transitions.
refresh = 5
# LCD stays off until a threshold is breached; shows only the breached monitor(s).
only_alert = false

[alerts]
# Thresholds that trigger buzzer alerts.
temp_warn = 80   # °C — CPU temperature
cpu_warn  = 90   # %  — sustained CPU usage
mem_warn  = 90   # %  — RAM usage
bat_warn  = 40   # %  — battery percentage below which to warn (battery-powered only)
